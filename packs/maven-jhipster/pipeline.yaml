extends:
  file: ../maven/pipeline.yaml
pipelines:
  release:
    pipeline:
      stages:
      - name: build
        steps:
        options:
          containerOptions:
            resources:
              limits:
                cpu: 1.0
                memory: 2048Mi
              requests:
                cpu: 0.1
                memory: 256Mi
